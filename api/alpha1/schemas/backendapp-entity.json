{
    "$schema": "http://json-schema.org/draft-07/schema",
    "$id": "backendapp-entity.json",
    "title": "BackendApp",
    "description": "Entities of the kind 'BackendApp' are sets of backend components that are released (launched) together to implement a single functional scope",
    "type": "object",
    "allOf": [
      { "$ref": "entity.json" },
      {
        "properties": {
          "kind": { "const": "backendapp" },
          "spec": {
            "$ref": "backendapp-spec.json"
          }
        }
      }
    ]
  }
