{
  "$schema": "http://json-schema.org/draft-07/schema",
  "$id": "queue-spec.json",
  "title": "Queue Spec",
  "description": "The \"spec\" field of entities of kind \"queue\"",
  "type": "object",
  "allOf": [
    { "$ref": "component-spec.json" }
  ],
  "properties": {
    "application": {
      "$ref": "backendapp-ref.json"
    },
    "type": {
      "description": "indicates the type of queue, for example `sqs', 'rabbitmq' or 'kafka'.",
      "type": "string"
    }
  },
  "required": ["application", "type"]
}
