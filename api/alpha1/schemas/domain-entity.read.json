{
  "$schema": "http://json-schema.org/draft-07/schema",
  "$id": "domain-entity.read.json",
  "title": "Domain",
  "description": "An entity that acts as a container for applications that fulfill the same functional scope but that are released separately to different platforms. Often domains align with coarse business functionality.",
  "type": "object",
  "allOf": [
    { "$ref": "entity.read.json" },
    {
      "properties": {
        "kind": { "const": "domain" },
        "spec": {
          "$ref": "domain-spec.read.json"
        }
      }
    }
  ]
}
