{
  "$schema": "http://json-schema.org/draft-07/schema",
  "$id": "consumer-entity.json",
  "title": "Consumer",
  "description": "A 'Consumer' entity represents a backend process that listens to topics or queues and consumes the available messages there.",
  "type": "object",
  "allOf": [
    { "$ref": "entity.json" },
    {
      "properties": {
        "kind": { "const": "consumer" },
        "spec": {
          "$ref": "consumer-spec.json"
        }
      }
    }
  ]
}
