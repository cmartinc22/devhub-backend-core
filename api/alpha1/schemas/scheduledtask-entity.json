{
  "$schema": "http://json-schema.org/draft-07/schema",
  "$id": "scheduledtask-entity.json",
  "title": "ScheduledTask",
  "description": "A 'ScheduledTask' entity represents a backend process that is started either periodically or only once at specific date and it stops when it is finished.",
  "type": "object",
  "allOf": [
    { "$ref": "entity.json" },
    {
      "properties": {
        "kind": { "const": "scheduledtask" },
        "spec": {
          "$ref": "scheduledtask-spec.json"
        }
      }
    }
  ]
}
